<%= form_with(model: sticker) do |form| %>
  <% if sticker.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(sticker.errors.count, "error") %> prohibited this sticker from being saved:</h2>

      <ul>
        <% sticker.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :member_id, style: "display: block" %>
    <%= form.text_field :member_id %>
  </div>

  <div>
    <%= form.label :sticker_number, style: "display: block" %>
    <%= form.number_field :sticker_number %>
  </div>

  <div>
    <%= form.label :sticker_variation, style: "display: block" %>
    <%= form.text_field :sticker_variation %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
